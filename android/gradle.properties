org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
# `true` casse `flutter_plugin_android_lifecycle` (applique encore le
# plugin Kotlin classique, qu'AGP 9 refuse une fois le built-in Kotlin
# actif) : remis à `false`, valeur d'origine du template. Le seul module
# qui en a besoin (`file_picker`, dont les sources .kt ne compilaient
# plus) reçoit le plugin Kotlin classique via un correctif ciblé dans
# `android/build.gradle.kts` plutôt que d'activer le built-in Kotlin pour
# tout le projet.
android.builtInKotlin=false
